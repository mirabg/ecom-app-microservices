#spring:
#  cloud:
#    gateway:
#      # NOTE: Spring Cloud Gateway 5.x moved route config from
#      # `spring.cloud.gateway.routes` to `spring.cloud.gateway.server.webflux.routes`.
#      # The old prefix is silently ignored - the gateway starts with zero routes
#      # and every request 404s.
#      server:
#        webflux:
#          routes:
#            - id: product-service
#              uri: lb://product-service
#              predicates:
#                - Path=/api/products/**
#            - id: user-service
#              uri: lb://user-service
#              predicates:
#                - Path=/api/users/**
#            # Comma-separated patterns map to the Path predicate's `patterns` list.
#            # Do NOT put a space after the comma - it becomes part of the pattern.
#            - id: order-service
#              uri: lb://order-service
#              predicates:
#                - Path=/api/orders/**,/api/cart/**
#            # Eureka does not register with itself (register-with-eureka: false
#            # in eureka/application.yaml), so it never appears as a load-balanced
#            # instance - `lb://` can't resolve it. Route directly to its host:port
#            # instead. Two routes are needed because the dashboard HTML is served
#            # at "/" and its CSS/JS are linked with paths relative to the current
#            # URL (e.g. "eureka/css/wro.css", no leading slash), so a browser at
#            # /eureka/dashboard resolves them as /eureka/eureka/css/wro.css.
#            - id: eureka-dashboard
#              uri: ${EUREKA_DASHBOARD_URL:http://localhost:8761}
#              predicates:
#                - Path=/eureka/dashboard
#              filters:
#                - SetPath=/
#            - id: eureka-dashboard-assets
#              uri: ${EUREKA_DASHBOARD_URL:http://localhost:8761}
#              predicates:
#                - Path=/eureka/eureka/**
#              filters:
#                - StripPrefix=1

server:
  port: 8080

# `lb://<service-id>` resolves via Eureka, so the gateway must be a registered client.
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    # The gateway actuator endpoint defaults to access=none, so
    # /actuator/gateway/routes 404s even when exposed via the wildcard above.
    # read-only permits GET (inspect resolved routes) but not POST (refresh).
    gateway:
      access: read-only
      health:
        show-details: always
  health:
    circuitbreakers:
      enabled: true
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_TRACING_ENDPOINT:http://localhost:9411/api/v2/spans}

logging:
  level:
    root: INFO

# Circuit Breaker / Rate Limiter Configuration
resilience4j:
  circuitbreaker:
    instances:
      gatewayCircuitBreaker:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 10s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
        automaticTransitionFromOpenToHalfOpenEnabled: true
        slidingWindowType: COUNT_BASED
  # Applied globally in RateLimiterFilter.
  ratelimiter:
    instances:
      gatewayRateLimiter:
        registerHealthIndicator: true
        # Allow this many requests per refresh period...
        limitForPeriod: 20
        # ...refreshed every second (i.e. ~20 requests/sec sustained).
        limitRefreshPeriod: 1s
        # How long a request waits for a permit to free up before being
        # rejected with 429, rather than failing immediately.
        timeoutDuration: 0

