spring:
  application:
    name: user-service
  config:
    # No `optional:` prefix - an unreachable config server must fail loudly
    # rather than let the app boot with no mongo/eureka/keycloak config.
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 2000
        multiplier: 1.1
        max-interval: 10000
logging:
  level:
    root: INFO
    com.ecom.user: DEBUG
  file:
    name: logs/${spring.application.name}.log