spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
server:
  port: 0
# Tests must not export spans to a real Zipkin collector
management:
  tracing:
    enabled: false
