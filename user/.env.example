# Copy to user/.env and adjust for your environment:  cp user/.env.example user/.env
# user/.env is git-ignored and must never be committed.
#
# user-service resolves ${MONGO_URL} from config-server's user-service.yaml,
# so this variable must be present in the environment before the service starts.
MONGO_URL=mongodb://root:root@localhost:27017/

